# Sanitized Notebook Demo Example

This folder contains a small demo script pattern for OpenShift AI or a notebook workspace.

It is intentionally simple:
- call the public app metadata route
- call the internal KServe endpoint
- write a human-readable summary

The point is not notebook sophistication. The point is making demos repeatable.
